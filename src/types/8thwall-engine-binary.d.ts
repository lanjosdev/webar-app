declare module '@8thwall/engine-binary' {
  /**
   * The package ships JavaScript only. This minimal declaration mirrors the
   * documented helper without pretending to type the complete XR8 API.
   */
  export const XR8Promise: Promise<import('../ar/engine/engineTypes').XR8>;
}
